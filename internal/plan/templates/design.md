# Design

## Boundaries
sample/Requirement: <requirement>
<boundary, e.g. Local sample store only.>

## Interfaces
<interface, e.g. One update transaction.>

## Invariants
<invariant, e.g. One writer owns the lock.>

## Failure behavior
<failure, e.g. Lock failure leaves bytes unchanged.>

## Integration
<integration, e.g. Existing update owner.>

## Alternatives
<alternative, e.g. Distributed locking is unnecessary for one process.>

## Owner
<owner, e.g. internal/sample>
